You are GitHub Copilot inside Visual Studio 2022.

Open MainWindow.xaml.cs and update the code-behind.

Requirements:
• Set DataContext = new MainViewModel() in the constructor after InitializeComponent().
• Ensure that MainWindow.xaml’s ContentControl is bound to CurrentViewModel using DataTemplates for MapViewModel and BattleViewModel.
• Add the DataTemplates in MainWindow.xaml within Window.Resources:

    <DataTemplate DataType="{x:Type vm:MapViewModel}">
        <v:MapView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:BattleViewModel}">
        <v:BattleView />
    </DataTemplate>

• (Assume xmlns:vm="clr-namespace:SimpleRPG.ViewModels" and xmlns:v="clr-namespace:SimpleRPG.Views")
• Comment clearly where to later add fade transitions between views.


---

After you finish, update the Readme.md file with the changes.