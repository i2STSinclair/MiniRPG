You are GitHub Copilot inside Visual Studio 2022.

Edit GameService.cs.

Requirements:
• Add static method GetRandomLoot():
   - Randomly return an Item from Item.GetSampleItems()
   - 50% chance to drop nothing (return null)
• Example:
   var items = Item.GetSampleItems();
   return rand.NextDouble() > 0.5 ? items[rand.Next(items.Count)] : null;
• Add TODO: // Add rarity tiers and loot tables later

---

After you finish, update the Readme.md file with the changes.