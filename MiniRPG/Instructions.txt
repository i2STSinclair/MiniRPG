You are GitHub Copilot inside Visual Studio 2022.

Edit MainViewModel.cs.

Requirements:
• On construction:
   - Attempt to load existing player via SaveLoadService.LoadPlayer().
   - If null, create a new Player.
   - Assign to CurrentPlayer.
• Add SaveCommand using RelayCommand.
• When SaveCommand executes, call SaveLoadService.SavePlayer(CurrentPlayer).
• Add TODO: // Later - trigger auto-save after each battle or major event


---

After you finish, update the Readme.md file with the changes.