You are GitHub Copilot inside Visual Studio 2022.

Modify BattleViewModel.cs.

Requirements:
• After awarding EXP on victory:
   - var loot = GameService.GetRandomLoot();
   - If (loot != null):
       * Player.AddItem(loot);
       * Append $"You found {loot.Name}!"
   - Else append "No items found this time."
• Call SaveLoadService.SavePlayer(Player) after loot assignment.
• Add TODO: // Display loot visually in post-battle summary later

---

After you finish, update the Readme.md file with the changes.