You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add properties:
   - int Level (default 1)
   - int Experience (default 0)
   - int ExperienceToNextLevel (default 10)
• Add method GainExperience(int amount):
   - Experience += amount
   - If Experience >= ExperienceToNextLevel:
       * Level++
       * Experience -= ExperienceToNextLevel
       * ExperienceToNextLevel = (int)(ExperienceToNextLevel * 1.5)
       * MaxHP += 5
       * Attack += 1
       * Defense += 1
       * HP = MaxHP
   - Return true if level-up occurred, false otherwise.
• Add TODO: // Later - play level-up animation, add ability points, and save to file


---

After you finish, update the Readme.md file with the changes.