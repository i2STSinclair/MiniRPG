You are GitHub Copilot inside Visual Studio 2022.

Modify MainViewModel.cs.

Requirements:
• When showing TitleViewModel, call AudioService.PlayTitleTheme()
• When switching to MapViewModel, call AudioService.PlayMapTheme()
• When switching to BattleViewModel, call AudioService.PlayBattleTheme()
• Use try/catch to prevent exceptions if audio files missing.
• Add TODO: // Replace with smooth fade transitions between tracks

---

After you finish, update the Readme.md file with the changes.