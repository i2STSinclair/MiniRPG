You are GitHub Copilot inside Visual Studio 2022.

Modify MainViewModel.cs to include:
• ObservableCollection<string> GlobalLog
• Method AddLog(string message)
• Whenever switching between MapViewModel and BattleViewModel, pass a reference to GlobalLog so both can append messages.

Update MainWindow.xaml:
• Bind the bottom TextBox to ItemsSource="{Binding GlobalLog}" or use Text="{Binding CombinedLog}" if you prefer a concatenated string.
• Make it read-only, multi-line, vertical scroll visible.
• Add comment placeholder for styling later (<!-- TODO: Style log box with pixel-art frame -->)


---

After you finish, update the Readme.md file with the changes.