You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add ObservableCollection<Item> Inventory { get; set; } = new();
• Add method AddItem(Item item):
   - Inventory.Add(item);
   - Optionally Debug.WriteLine($"{item.Name} added to inventory.")
• Ensure Inventory is serialized properly in SaveLoadService (JsonSerializer supports ObservableCollection automatically).
• Add TODO: // Add inventory capacity and sorting later

---

After you finish, update the Readme.md file with the changes.