You are GitHub Copilot inside Visual Studio 2022.

Modify MainViewModel.cs.

Requirements:
• On startup, set CurrentViewModel = new TitleViewModel().
• Subscribe to TitleSelectionMade event:
   - If "New": create new Player() and new MapViewModel(CurrentPlayer)
   - If "Continue": try SaveLoadService.LoadPlayer() → if null, create new Player()
• Then set CurrentViewModel to MapViewModel.
• Keep GlobalLog available and append "Welcome back!" or "A new adventure begins!"
• Add TODO: // Add intro cutscene for new game start

---

After you finish, update the Readme.md file with the changes.