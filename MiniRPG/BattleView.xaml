<UserControl x:Class="MiniRPG.BattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>

        <!-- Top: Battle status -->
        <TextBlock Grid.Row="0" Name="BattleStatusText" Text="Battle in Progress" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10" />

        <!-- Center: Action buttons and sprites -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10">
            <!-- TODO: Player sprite -->
            <Button Name="AttackButton" Content="Attack" Command="{Binding AttackCommand}" Width="120" Margin="0,5" />
            <Button Name="DefendButton" Content="Defend" Command="{Binding DefendCommand}" Width="120" Margin="0,5" />
            <Button Name="RunButton" Content="Run" Command="{Binding RunCommand}" Width="120" Margin="0,5" />
            <!-- TODO: Enemy sprite -->
        </StackPanel>

        <!-- Bottom: Combat log -->
        <TextBox Grid.Row="2" Name="CombatLogTextBox" Text="{Binding CombatLog}" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="13" />
    </Grid>
</UserControl>
